{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-4">
                <div class="card">
                    <div class="card-body">
                        {{ form_start(conversationForm) }}
                        {{ form_row(conversationForm.title) }}
                        <div class="text-end">
                            <button type="submit" class="btn btn-outline-primary">{{ 'save'|trans }}</button>
                        </div>
                        {{ form_end(conversationForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

