<turbo-stream action="update" target="bookmark-btn">
    <template>
        <a class="btn {{ app.user.hasItemBeenBookmarked(item) ? 'btn-primary' : 'btn-outline-primary' }}"
           href="{{ path('create_item_bookmark', {id: item.id}) }}">
            <span class="mdi mdi-bookmark-outline"></span>
        </a>
    </template>
</turbo-stream>