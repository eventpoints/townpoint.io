{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-8">
                <turbo-frame id="new-event-form-frame">
                    <div class="card">
                        <div class="card-body">
                            <h5>{{ 'create-event'|trans }}</h5>
                            {{ include('/event/_form.html.twig') }}
                        </div>
                    </div>
                </turbo-frame>
            </div>
        </div>
    </div>
{% endblock %}