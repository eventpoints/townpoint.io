{{ form_start(eventForm) }}
{{ form_row(eventForm.title) }}
{{ form_row(eventForm.address) }}
<div class="row">
    <div class="col-6">
        {{ form_row(eventForm.startAt) }}
    </div>
    <div class="col-6">
        {{ form_row(eventForm.endAt) }}
    </div>
    <div class="text-end">
        <button type="submit" class="btn btn-outline-primary">{{ 'create'|trans }}</button>
    </div>
</div>
{{ form_end(eventForm) }}