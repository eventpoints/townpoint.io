{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row g-3 justify-content-center">
            <div class="col-12 col-md-3">
                <div class="card mb-3">
                    <turbo-frame id="show-event-user-ticket-frame">
                        <img src="{{ ticketQr }}" class="card-img-top bg-transparent" alt="...">
                    </turbo-frame>
                </div>
            </div>
        </div>

    </div>
{% endblock %}

